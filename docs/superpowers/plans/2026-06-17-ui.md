# UI de escritorio (WPF) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir la UI completa de la V1 en WPF (Fluent moderno), con foco máximo en mostrar el avance dentro de la app, corriendo el pipeline in-process.

**Architecture:** Proyecto `Resumenes.Ui` (WPF, MVVM con CommunityToolkit.Mvvm, look WPF-UI) que referencia `Core`/`Infrastructure`. Se refactoriza el backbone para reportar progreso (`IProgress<ProgresoPaso>`), se extrae la orquestación de 3 fases a `ServicioAnalisis` (usado por Cli y UI), y se agrega historial (`ListarAnalisis`). La UI corre Fase 1 → Fase 2 → pausa para confirmar temas → Fase 3.

**Tech Stack:** .NET 9 (C#), WPF (`net9.0-windows`), WPF-UI (lepoco/wpfui), CommunityToolkit.Mvvm, xUnit. Reusa Microsoft.Data.Sqlite, Polly, Serilog, PaddleOCR/LibreOffice/Deepseek del backbone.

## Global Constraints

- Todo bajo `D:\Desarrollo\Programacion\Resumenes`. Rutas relativas a esa raíz.
- **Sin git:** OMITIR todo paso de commit; el plan no usa control de versiones.
- Frameworks: backbone en `net9.0`; UI en `net9.0-windows` (WPF).
- MVVM estricto: sin lógica de negocio en code-behind; los ViewModels usan `CommunityToolkit.Mvvm` (`[ObservableProperty]`, `[RelayCommand]`).
- Compatibilidad: el `IProgress<ProgresoPaso>` del orquestador es OPCIONAL (default no-op) para no romper la consola ni los 16 tests existentes.
- Idioma de código/comentarios: español (identificadores en español salvo términos técnicos).
- Encoding UTF-8 punta a punta.
- El pipeline corre en background; el progreso se marshalea al hilo de UI con `IProgress<T>`.
- No placeholders: cada paso trae el código real.

---

## File Structure

```
src/Resumenes.Core/
  Orquestacion/ProgresoPaso.cs            (FaseAnalisis, EstadoEvento, ProgresoPaso, ContextoPaso)
  Orquestacion/PasoPipeline.cs            (MOD: Ejecutar recibe ContextoPaso)
  Orquestacion/PipelineOrquestador.cs     (MOD: acepta IProgress<ProgresoPaso>?, reporta etapas)
  Interfaces/IServicioOcr.cs              (MOD: OcrAsync recibe IProgress<(int,int)>? subProgreso)
  Interfaces/IRasterizador.cs             (MOD: idem subProgreso opcional)
  Interfaces/IRepositorioEstado.cs        (MOD: + ListarAnalisis())
  Interfaces/IServicioAnalisis.cs         (nuevo: 3 fases)
src/Resumenes.Infrastructure/
  Aplicacion/ServicioAnalisis.cs          (nuevo: orquesta las 3 fases + progreso)
  Ocr/PaddleOcrServicio.cs                (MOD: reporta página i/N)
  Ocr/PyMuPdfRasterizador.cs              (MOD: firma subProgreso, no-op)
  Persistencia/SqliteRepositorioEstado.cs (MOD: ListarAnalisis())
src/Resumenes.Cli/
  Program.cs                              (MOD: usa ServicioAnalisis)
  ConstructorPipeline.cs                  (MOD: pasos reciben ContextoPaso)
  DetectorTemas.cs                        (se mueve la lógica a ServicioAnalisis o se reusa)
src/Resumenes.Ui/                          (NUEVO proyecto WPF)
  Resumenes.Ui.csproj
  App.xaml / App.xaml.cs                  (DI, tema, arranque)
  MainWindow.xaml / .cs                   (NavigationView shell)
  Servicios/ServicioNavegacion.cs
  Servicios/MapeoProgreso.cs              (ProgresoPaso -> estado de UI)
  Controles/LoaderPensante.xaml/.cs       (animación "pensante")
  Vistas/ (Inicio, Configurar, Ejecutando, ConfirmarTemas, Generando, Resultados, Configuracion, Onboarding) .xaml/.cs
  ViewModels/ (uno por vista) + ItemProgresoVm, TemaEditableVm, AnalisisHistorialVm
tests/Resumenes.Tests/
  ProgresoOrquestadorTests.cs, ServicioAnalisisTests.cs, ListarAnalisisTests.cs
tests/Resumenes.Ui.Tests/                  (NUEVO: tests de ViewModels)
  Resumenes.Ui.Tests.csproj
  (VM tests con ServicioAnalisis fakeado)
```

---

## Parte A — Refactor del backbone (TDD)

### Task 1: Modelo de progreso + ContextoPaso

**Files:**
- Create: `src/Resumenes.Core/Orquestacion/ProgresoPaso.cs`
- Test: `tests/Resumenes.Tests/ProgresoOrquestadorTests.cs` (se crea en Task 3)

**Interfaces:**
- Produces: `enum FaseAnalisis { Limpieza, Deteccion, Generacion }`; `enum EstadoEvento { Iniciado, Avance, Completado, Salteado, Error }`; `record ProgresoPaso(FaseAnalisis Fase, string Item, int ItemIndice, int ItemTotal, Etapa Etapa, string Detalle, int? SubIndice, int? SubTotal, EstadoEvento Estado)`; `class ContextoPaso` con `Reportar(string detalle, int? sub=null, int? subTotal=null)`.

- [ ] **Step 1: Crear el archivo**

Create `src/Resumenes.Core/Orquestacion/ProgresoPaso.cs`:

```csharp
using Resumenes.Core.Modelos;

namespace Resumenes.Core.Orquestacion;

public enum FaseAnalisis { Limpieza, Deteccion, Generacion }
public enum EstadoEvento { Iniciado, Avance, Completado, Salteado, Error }

public record ProgresoPaso(
    FaseAnalisis Fase,
    string Item,
    int ItemIndice,
    int ItemTotal,
    Etapa Etapa,
    string Detalle,
    int? SubIndice,
    int? SubTotal,
    EstadoEvento Estado);

// Se entrega a cada paso para que reporte sub-progreso real (p. ej. OCR página i/N).
public sealed class ContextoPaso(
    IProgress<ProgresoPaso>? progreso, FaseAnalisis fase, string item, int itemIndice, int itemTotal, Etapa etapa)
{
    public CancellationToken Ct { get; init; }
    public void Reportar(string detalle, int? sub = null, int? subTotal = null) =>
        progreso?.Report(new ProgresoPaso(fase, item, itemIndice, itemTotal, etapa, detalle, sub, subTotal, EstadoEvento.Avance));
}
```

- [ ] **Step 2: Compilar**

Run: `dotnet build src/Resumenes.Core`
Expected: `Build succeeded`.

---

### Task 2: `ListarAnalisis()` en el repositorio (TDD)

**Files:**
- Modify: `src/Resumenes.Core/Interfaces/IRepositorioEstado.cs`
- Modify: `src/Resumenes.Infrastructure/Persistencia/SqliteRepositorioEstado.cs`
- Test: `tests/Resumenes.Tests/ListarAnalisisTests.cs`

**Interfaces:**
- Produces: `IReadOnlyList<Analisis> IRepositorioEstado.ListarAnalisis()` (ordenado por `actualizado_en` desc).

- [ ] **Step 1: Agregar a la interfaz**

En `src/Resumenes.Core/Interfaces/IRepositorioEstado.cs`, dentro de la interfaz, agregar:

```csharp
    IReadOnlyList<Analisis> ListarAnalisis();
```

- [ ] **Step 2: Escribir el test que falla**

Create `tests/Resumenes.Tests/ListarAnalisisTests.cs`:

```csharp
using Resumenes.Core.Modelos;
using Resumenes.Infrastructure.Persistencia;
using Xunit;

namespace Resumenes.Tests;

public class ListarAnalisisTests : IDisposable
{
    private readonly string _ruta = Path.Combine(Path.GetTempPath(), $"resu_{Guid.NewGuid():N}.sqlite");
    private SqliteRepositorioEstado Crear()
    {
        var repo = new SqliteRepositorioEstado($"Data Source={_ruta}");
        repo.InicializarEsquema();
        return repo;
    }

    [Fact]
    public void Lista_analisis_ordenados_por_actualizado_desc()
    {
        var repo = Crear();
        repo.GuardarAnalisis(new Analisis("a1", "Uno", "c1", "fp1", EstadoAnalisis.Completado,
            new DateTime(2026, 1, 1), new DateTime(2026, 1, 1)));
        repo.GuardarAnalisis(new Analisis("a2", "Dos", "c2", "fp2", EstadoAnalisis.EnProceso,
            new DateTime(2026, 1, 2), new DateTime(2026, 1, 2)));

        var lista = repo.ListarAnalisis();

        Assert.Equal(2, lista.Count);
        Assert.Equal("a2", lista[0].Id); // más reciente primero
        Assert.Equal("a1", lista[1].Id);
    }

    public void Dispose()
    {
        Microsoft.Data.Sqlite.SqliteConnection.ClearAllPools();
        if (File.Exists(_ruta)) File.Delete(_ruta);
    }
}
```

- [ ] **Step 3: Correr el test y verificar que falla**

Run: `dotnet test tests/Resumenes.Tests --filter ListarAnalisisTests`
Expected: FAIL (no implementado).

- [ ] **Step 4: Implementar en SqliteRepositorioEstado**

En `src/Resumenes.Infrastructure/Persistencia/SqliteRepositorioEstado.cs`, agregar el método:

```csharp
    public IReadOnlyList<Analisis> ListarAnalisis()
    {
        using var con = Abrir();
        using var cmd = con.CreateCommand();
        cmd.CommandText = @"SELECT id, nombre, carpeta_origen, fingerprint, estado, creado_en, actualizado_en
                            FROM Analisis ORDER BY actualizado_en DESC;";
        using var r = cmd.ExecuteReader();
        var lista = new List<Analisis>();
        while (r.Read())
            lista.Add(new Analisis(r.GetString(0), r.GetString(1), r.GetString(2), r.GetString(3),
                Enum.Parse<EstadoAnalisis>(r.GetString(4)), DateTime.Parse(r.GetString(5)), DateTime.Parse(r.GetString(6))));
        return lista;
    }
```

- [ ] **Step 5: Correr el test y verificar que pasa**

Run: `dotnet test tests/Resumenes.Tests --filter ListarAnalisisTests`
Expected: PASS.

---

### Task 3: Progreso en el orquestador + ContextoPaso en los pasos (TDD)

**Files:**
- Modify: `src/Resumenes.Core/Orquestacion/PasoPipeline.cs`
- Modify: `src/Resumenes.Core/Orquestacion/PipelineOrquestador.cs`
- Modify: `tests/Resumenes.Tests/Fakes/Fakes.cs` (si hace falta), `PipelineOrquestadorTests.cs`
- Create: `tests/Resumenes.Tests/ProgresoOrquestadorTests.cs`

**Interfaces:**
- Consumes: `ProgresoPaso`, `ContextoPaso` (Task 1).
- Produces: `PasoPipeline.Ejecutar` pasa a `Func<ContextoPaso, Task>`; `PipelineOrquestador.EjecutarAsync(string analisisId, IReadOnlyList<PasoPipeline> pasos, CancellationToken ct, FaseAnalisis fase = FaseAnalisis.Limpieza, string item = "", int itemIndice = 0, int itemTotal = 0, IProgress<ProgresoPaso>? progreso = null)` que emite Iniciado/Completado/Salteado/Error por etapa.

- [ ] **Step 1: Cambiar `PasoPipeline.Ejecutar` a recibir `ContextoPaso`**

En `src/Resumenes.Core/Orquestacion/PasoPipeline.cs`, cambiar el record:

```csharp
public record PasoPipeline(
    Etapa Etapa,
    string? ArchivoId,
    string? TemaId,
    string? RutaArtefacto,
    Func<CancellationToken, Task<string>> CalcularHashEntrada,
    Func<ContextoPaso, Task> Ejecutar,
    string? PromptVersion = null,
    string? ModeloIa = null);
```

- [ ] **Step 2: Escribir el test de progreso que falla**

Create `tests/Resumenes.Tests/ProgresoOrquestadorTests.cs`:

```csharp
using Resumenes.Core.Modelos;
using Resumenes.Core.Orquestacion;
using Resumenes.Tests.Fakes;
using Xunit;

namespace Resumenes.Tests;

public class ProgresoOrquestadorTests
{
    [Fact]
    public async Task Reporta_iniciado_y_completado_por_paso()
    {
        var repo = new RepositorioEnMemoria();
        var orq = new PipelineOrquestador(repo, new RelojFijo());
        var eventos = new List<ProgresoPaso>();
        var progreso = new Progress<ProgresoPaso>(e => eventos.Add(e));

        var paso = new PasoPipeline(Etapa.OcrBruto, "arc1", null, "r.txt",
            _ => Task.FromResult("h1"),
            ctx => { ctx.Reportar("OCR", 1, 3); return Task.CompletedTask; });

        // Progress<T> entrega de forma asíncrona; capturamos con un TaskCompletionSource simple.
        await orq.EjecutarAsync("an1", new[] { paso }, default,
            FaseAnalisis.Limpieza, "arc1", 1, 1, progreso);
        await Task.Delay(50);

        Assert.Contains(eventos, e => e.Estado == EstadoEvento.Iniciado && e.Etapa == Etapa.OcrBruto);
        Assert.Contains(eventos, e => e.Estado == EstadoEvento.Avance && e.SubIndice == 1 && e.SubTotal == 3);
        Assert.Contains(eventos, e => e.Estado == EstadoEvento.Completado && e.Etapa == Etapa.OcrBruto);
    }
}
```

- [ ] **Step 3: Correr y verificar que falla**

Run: `dotnet test tests/Resumenes.Tests --filter ProgresoOrquestadorTests`
Expected: FAIL (firma vieja / no compila).

- [ ] **Step 4: Implementar el progreso en el orquestador**

Reemplazar `src/Resumenes.Core/Orquestacion/PipelineOrquestador.cs` por:

```csharp
using Resumenes.Core.Interfaces;
using Resumenes.Core.Modelos;

namespace Resumenes.Core.Orquestacion;

public class PipelineOrquestador(IRepositorioEstado repo, IRelojUtc reloj)
{
    public async Task<ResultadoEjecucion> EjecutarAsync(
        string analisisId, IReadOnlyList<PasoPipeline> pasos, CancellationToken ct,
        FaseAnalisis fase = FaseAnalisis.Limpieza, string item = "", int itemIndice = 0, int itemTotal = 0,
        IProgress<ProgresoPaso>? progreso = null)
    {
        int ok = 0, salteados = 0, errores = 0;
        var mensajes = new List<string>();

        void Emitir(Etapa etapa, EstadoEvento estado, string detalle) =>
            progreso?.Report(new ProgresoPaso(fase, item, itemIndice, itemTotal, etapa, detalle, null, null, estado));

        foreach (var paso in pasos)
        {
            ct.ThrowIfCancellationRequested();
            var existente = repo.ObtenerUnidad(analisisId, paso.ArchivoId, paso.TemaId, paso.Etapa);
            var hash = await paso.CalcularHashEntrada(ct);

            if (existente is { Estado: EstadoUnidad.Completado } &&
                (existente.HashEntrada == hash || existente.FijadoPorUsuario))
            {
                salteados++;
                Emitir(paso.Etapa, EstadoEvento.Salteado, "reutilizado");
                continue;
            }

            var unidad = existente ?? new Unidad
            {
                AnalisisId = analisisId, ArchivoId = paso.ArchivoId, TemaId = paso.TemaId, Etapa = paso.Etapa
            };

            try
            {
                unidad.Estado = EstadoUnidad.EnProceso;
                unidad.ActualizadoEn = reloj.Ahora();
                repo.GuardarUnidad(unidad);
                Emitir(paso.Etapa, EstadoEvento.Iniciado, "");

                var ctx = new ContextoPaso(progreso, fase, item, itemIndice, itemTotal, paso.Etapa) { Ct = ct };
                await paso.Ejecutar(ctx);

                unidad.Estado = EstadoUnidad.Completado;
                unidad.HashEntrada = hash;
                unidad.RutaArtefacto = paso.RutaArtefacto;
                unidad.PromptVersion = paso.PromptVersion;
                unidad.ModeloIa = paso.ModeloIa;
                unidad.ErrorMsg = null;
                unidad.ActualizadoEn = reloj.Ahora();
                repo.GuardarUnidad(unidad);
                ok++;
                Emitir(paso.Etapa, EstadoEvento.Completado, "");
            }
            catch (Exception ex)
            {
                errores++;
                mensajes.Add($"{paso.Etapa}: {ex.Message}");
                try
                {
                    unidad.Estado = EstadoUnidad.Error;
                    unidad.ErrorMsg = ex.Message;
                    unidad.ActualizadoEn = reloj.Ahora();
                    repo.GuardarUnidad(unidad);
                }
                catch { }
                Emitir(paso.Etapa, EstadoEvento.Error, ex.Message);
                break;
            }
        }
        return new ResultadoEjecucion(ok, salteados, errores, mensajes);
    }
}
```

- [ ] **Step 5: Adaptar los tests existentes del orquestador**

En `tests/Resumenes.Tests/PipelineOrquestadorTests.cs`, los pasos usan ahora `Func<ContextoPaso, Task>`. Cambiar las lambdas de `_ => { ... return Task.CompletedTask; }` por `ctx => { ... return Task.CompletedTask; }` y `_ => throw ...` por `ctx => throw ...`. (El `CalcularHashEntrada` sigue siendo `Func<CancellationToken,Task<string>>`.) Ejemplo del helper `Paso`:

```csharp
    private static PasoPipeline Paso(string hash, Action onEjecutar, Etapa etapa = Etapa.LimpiezaIA)
        => new(etapa, "arc1", null, "ruta.txt",
            _ => Task.FromResult(hash),
            ctx => { onEjecutar(); return Task.CompletedTask; });
```

Y en el test de error, el paso que lanza: `ctx => throw new InvalidOperationException("boom")`.

- [ ] **Step 6: Correr toda la suite**

Run: `dotnet test tests/Resumenes.Tests`
Expected: PASS (los 16 previos adaptados + ProgresoOrquestadorTests).

---

### Task 4: Sub-progreso de OCR/rasterizado + firma en interfaces

**Files:**
- Modify: `src/Resumenes.Core/Interfaces/IServicioOcr.cs`, `IRasterizador.cs`
- Modify: `src/Resumenes.Infrastructure/Ocr/PaddleOcrServicio.cs`, `PyMuPdfRasterizador.cs`
- Modify: `tests/Resumenes.Tests/Fakes/Fakes.cs` (si hay fakes de estos)

**Interfaces:**
- Produces: `IServicioOcr.OcrAsync(IReadOnlyList<string> rutasImagenes, CancellationToken ct, IProgress<(int actual, int total)>? subProgreso = null)`; `IRasterizador.RasterizarAsync(string pdfPath, string outDir, int dpi, CancellationToken ct, IProgress<(int actual, int total)>? subProgreso = null)`.

- [ ] **Step 1: Actualizar las interfaces**

`src/Resumenes.Core/Interfaces/IServicioOcr.cs`:

```csharp
namespace Resumenes.Core.Interfaces;

public interface IServicioOcr
{
    Task<string> OcrAsync(IReadOnlyList<string> rutasImagenes, CancellationToken ct,
        IProgress<(int actual, int total)>? subProgreso = null);
}
```

`src/Resumenes.Core/Interfaces/IRasterizador.cs`:

```csharp
namespace Resumenes.Core.Interfaces;

public interface IRasterizador
{
    Task<IReadOnlyList<string>> RasterizarAsync(string pdfPath, string outDir, int dpi, CancellationToken ct,
        IProgress<(int actual, int total)>? subProgreso = null);
}
```

- [ ] **Step 2: Reportar página i/N en `PaddleOcrServicio`**

En `src/Resumenes.Infrastructure/Ocr/PaddleOcrServicio.cs`, cambiar la firma de `OcrAsync` para aceptar `IProgress<(int,int)>? subProgreso = null`, y dentro del bucle por imagen, tras recibir el `result` de cada `req_id`, agregar:

```csharp
                subProgreso?.Report((i + 1, rutasImagenes.Count));
```

(justo después de `textos.Add(m.Texto ?? "");`).

- [ ] **Step 3: Firma en `PyMuPdfRasterizador`**

En `src/Resumenes.Infrastructure/Ocr/PyMuPdfRasterizador.cs`, agregar el parámetro `IProgress<(int actual, int total)>? subProgreso = null` a `RasterizarAsync`; tras obtener la lista de imágenes (split de stdout), reportar el total:

```csharp
        var rutas = salida.Split('\n', StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries);
        subProgreso?.Report((rutas.Length, rutas.Length));
        return rutas;
```

- [ ] **Step 4: Compilar (los llamadores se actualizan en Task 5/6)**

Run: `dotnet build src/Resumenes.Core src/Resumenes.Infrastructure`
Expected: `Build succeeded` (los parámetros nuevos son opcionales).

---

### Task 5: `ServicioAnalisis` (3 fases + progreso) y refactor del Cli (TDD)

**Files:**
- Create: `src/Resumenes.Core/Interfaces/IServicioAnalisis.cs`
- Create: `src/Resumenes.Infrastructure/Aplicacion/ServicioAnalisis.cs`
- Modify: `src/Resumenes.Cli/ConstructorPipeline.cs` (los pasos usan `ContextoPaso`; el OCR reporta sub-progreso)
- Modify: `src/Resumenes.Cli/Program.cs` (usa `ServicioAnalisis`)
- Create: `tests/Resumenes.Tests/ServicioAnalisisTests.cs`

**Interfaces:**
- Consumes: todo lo anterior + `DetectorTemas`/`ConstructorPipeline`.
- Produces:
  - `IServicioAnalisis` con:
    - `Task<Analisis> AbrirOCrearAsync(string carpeta, CancellationToken ct)`
    - `Task<ResultadoLote> ProcesarArchivosAsync(Analisis an, IProgress<ProgresoPaso>? p, CancellationToken ct)`
    - `Task<IReadOnlyList<TemaDetectado>> DetectarTemasAsync(Analisis an, string promptTemas, CancellationToken ct)`
    - `Task<ResultadoLote> GenerarPorTemasAsync(Analisis an, IReadOnlyList<TemaDetectado> temas, IProgress<ProgresoPaso>? p, CancellationToken ct)`
  - `record ResultadoLote(int Ok, int Error, IReadOnlyList<string> Fallos)`.

- [ ] **Step 1: Crear la interfaz**

Create `src/Resumenes.Core/Interfaces/IServicioAnalisis.cs`:

```csharp
using Resumenes.Core.Modelos;
using Resumenes.Core.Orquestacion;

namespace Resumenes.Core.Interfaces;

public record ResultadoLote(int Ok, int Error, IReadOnlyList<string> Fallos);

public interface IServicioAnalisis
{
    Task<Analisis> AbrirOCrearAsync(string carpeta, CancellationToken ct);
    Task<ResultadoLote> ProcesarArchivosAsync(Analisis an, IProgress<ProgresoPaso>? progreso, CancellationToken ct);
    Task<IReadOnlyList<TemaDetectado>> DetectarTemasAsync(Analisis an, string promptTemas, CancellationToken ct);
    Task<ResultadoLote> GenerarPorTemasAsync(Analisis an, IReadOnlyList<TemaDetectado> temas, IProgress<ProgresoPaso>? progreso, CancellationToken ct);
}
```

> Nota: `TemaDetectado` vive hoy en `Resumenes.Cli`. Para que `Core` lo referencie, **mover `TemaDetectado` (record) a `Resumenes.Core/Modelos/TemaDetectado.cs`** (solo el record; la lógica de detección queda en `ServicioAnalisis`). Actualizar `using` en `DetectorTemas.cs`/`ConstructorPipeline.cs`.

- [ ] **Step 2: Mover `TemaDetectado` a Core**

Create `src/Resumenes.Core/Modelos/TemaDetectado.cs`:

```csharp
namespace Resumenes.Core.Modelos;

public record TemaDetectado(string Id, string Nombre, int Orden, List<string> Archivos);
```

Quitar la definición de `TemaDetectado` de `src/Resumenes.Cli/DetectorTemas.cs` y agregar `using Resumenes.Core.Modelos;` donde haga falta.

- [ ] **Step 3: Escribir el test del servicio (fase 1 con fakes)**

Create `tests/Resumenes.Tests/ServicioAnalisisTests.cs`:

```csharp
using Resumenes.Core.Interfaces;
using Resumenes.Core.Modelos;
using Resumenes.Core.Orquestacion;
using Resumenes.Infrastructure.Aplicacion;
using Resumenes.Tests.Fakes;
using Xunit;

namespace Resumenes.Tests;

public class ServicioAnalisisTests
{
    [Fact]
    public async Task ProcesarArchivos_emite_progreso_y_marca_ok()
    {
        // Carpeta temporal con 1 txt (camino TXT: sin OCR/Office).
        var dir = Path.Combine(Path.GetTempPath(), "sa_" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(dir);
        File.WriteAllText(Path.Combine(dir, "a.txt"), "contenido de prueba");
        var ws = Path.Combine(dir, "ws"); Directory.CreateDirectory(ws);
        try
        {
            var repo = new RepositorioEnMemoria();
            var servicio = ServicioAnalisisFactory.ParaTests(repo, ws); // arma el servicio con fakes
            var an = await servicio.AbrirOCrearAsync(dir, default);
            var eventos = new List<ProgresoPaso>();
            var r = await servicio.ProcesarArchivosAsync(an, new Progress<ProgresoPaso>(eventos.Add), default);
            await Task.Delay(50);

            Assert.Equal(1, r.Ok);
            Assert.Equal(0, r.Error);
            Assert.Contains(eventos, e => e.Fase == FaseAnalisis.Limpieza);
        }
        finally { if (Directory.Exists(dir)) Directory.Delete(dir, true); }
    }
}
```

> El `ServicioAnalisisFactory.ParaTests` arma un `ServicioAnalisis` con fakes (IClienteIA que devuelve el texto tal cual, IServicioOcr/IRasterizador que no se usan para TXT, IGeneradorPdf no-op, IConversorOffice no-op). Crear esos fakes en `tests/Resumenes.Tests/Fakes/Fakes.cs` (FakeClienteIA que devuelve `req.PromptUser`, FakeOcr, FakeRasterizador, FakeGeneradorPdf, FakeConversor) y el factory.

- [ ] **Step 4: Crear fakes faltantes + factory**

En `tests/Resumenes.Tests/Fakes/Fakes.cs` agregar:

```csharp
public class FakeClienteIA : IClienteIA
{
    public Func<SolicitudIA, string>? Responder { get; set; }
    public Task<RespuestaIA> CompletarAsync(SolicitudIA req, CancellationToken ct)
        => Task.FromResult(new RespuestaIA(Responder?.Invoke(req) ?? req.PromptUser, "stop", 1, 1, 2));
}
public class FakeRasterizador : IRasterizador
{
    public Task<IReadOnlyList<string>> RasterizarAsync(string p, string o, int dpi, CancellationToken ct,
        IProgress<(int,int)>? sp = null) => Task.FromResult<IReadOnlyList<string>>(Array.Empty<string>());
}
public class FakeOcr : IServicioOcr
{
    public Task<string> OcrAsync(IReadOnlyList<string> img, CancellationToken ct, IProgress<(int,int)>? sp = null)
        => Task.FromResult("");
}
public class FakeGeneradorPdf : IGeneradorPdf
{
    public Task GenerarAsync(string c, string pdf, string t, string s, CancellationToken ct)
    { File.WriteAllText(pdf, "PDF"); return Task.CompletedTask; }
}
public class FakeConversor : IConversorOffice
{
    public Task<string> ConvertirAPdfAsync(string o, string outDir, CancellationToken ct)
        => Task.FromResult(o);
}
```

Y `ServicioAnalisisFactory.ParaTests(repo, ws)` que construye `ServicioAnalisis` con esos fakes y una `Configuracion`-equivalente (las rutas runtime no se usan con TXT/fakes). (Definir el factory junto a los fakes.)

- [ ] **Step 5: Correr el test y verificar que falla**

Run: `dotnet test tests/Resumenes.Tests --filter ServicioAnalisisTests`
Expected: FAIL (no existe `ServicioAnalisis`).

- [ ] **Step 6: Implementar `ServicioAnalisis`**

Create `src/Resumenes.Infrastructure/Aplicacion/ServicioAnalisis.cs` que mueve la orquestación de las 3 fases (hoy en `Program.cs`) detrás de la interfaz. Recibe por ctor: `IRasterizador, IServicioOcr, IClienteIA, IGeneradorPdf, IConversorOffice, IRepositorioEstado, IRelojUtc` y la config (rutas/modelo/dpi/maxChars + carpeta workspace). Implementa:
- `AbrirOCrearAsync`: glob de la carpeta (pdf/doc/docx/ppt/pptx/txt), hashes, fingerprint, `ObtenerAnalisisPorFingerprint` ?? nuevo con id `SlugId(nombre)`, `GuardarAnalisis`. Devuelve el `Analisis` y guarda internamente la lista de archivos+hashes.
- `ProcesarArchivosAsync`: por archivo, `GuardarArchivo`, construye los pasos por-archivo (Captura/OcrBruto/LimpiezaIA) y los corre con `orq.EjecutarAsync(..., FaseAnalisis.Limpieza, nombre, i+1, total, progreso)`; tolerancia por-archivo; arma el dict archivo_id→limpioPath.
- `DetectarTemasAsync`: usa el `DetectorTemas` ya existente (mover su uso aquí) sobre los archivos limpios; devuelve los temas (también escribe temas.json + persiste Tema/TemaArchivo).
- `GenerarPorTemasAsync`: por tema, construye los pasos por-tema y los corre con `FaseAnalisis.Generacion`; tolerancia por-tema; al final fija `Analisis.estado`.

La lógica de construcción de pasos sigue en `ConstructorPipeline` (Task 5 Step 7 lo adapta a `ContextoPaso`). El `SlugId`/`TipoDe` se mueven a un helper compartido en `Core/Apoyos/Ids.cs` (extraídos de `Program.cs`) para que el servicio los use.

(Por extensión, el código completo de `ServicioAnalisis` replica el flujo actual de `Program.cs` fases 1–3; ver `Program.cs` vigente como fuente.)

- [ ] **Step 7: Adaptar `ConstructorPipeline` a `ContextoPaso` + sub-progreso OCR**

En `src/Resumenes.Cli/ConstructorPipeline.cs` (que se moverá/於 compartirá; mantener en Cli pero referenciado por Infrastructure NO es válido — **mover `ConstructorPipeline` y `DetectorTemas` a `Resumenes.Infrastructure/Aplicacion/`** para que `ServicioAnalisis` los use), cambiar cada `async ct => { ... }` por `async ctx => { ... }` usando `ctx.Ct` como token, y en la etapa OcrBruto pasar el sub-progreso:

```csharp
            EscrituraAtomica.Escribir(bruto, await ocr.OcrAsync(imagenes, ctx.Ct,
                new Progress<(int a, int t)>(p => ctx.Reportar($"OCR página {p.a}/{p.t}", p.a, p.t))));
```

y en Captura el rasterizado:

```csharp
            imagenes = await rasterizador.RasterizarAsync(fuente, imagenesDir, cfg.Dpi, ctx.Ct,
                new Progress<(int a, int t)>(p => ctx.Reportar($"rasterizando {p.a}/{p.t}", p.a, p.t)));
```

Las llamadas IA: `ctx.Reportar("pensando…")` antes de `await ia.CompletarAsync(...)`.

- [ ] **Step 8: Refactor `Program.cs` para usar `ServicioAnalisis`**

Reemplazar la orquestación inline de `Program.cs` por: construir `ServicioAnalisis` (con los adaptadores reales) y llamar `AbrirOCrearAsync` → `ProcesarArchivosAsync(progresoLog)` → `DetectarTemasAsync` → `GenerarPorTemasAsync(progresoLog)`, donde `progresoLog` es un `Progress<ProgresoPaso>` que escribe al log. Mantener el pre-vuelo, el `--set-key` y el `--temas`.

- [ ] **Step 9: Correr toda la suite**

Run: `dotnet test`
Expected: PASS (previos + ServicioAnalisisTests). Y `dotnet build` de la solución OK.

- [ ] **Step 10: Verificación end-to-end de regresión (manual)**

Run: `dotnet run --project src/Resumenes.Cli -- tests/fixtures/temas`
Expected: mismo resultado que Fase 4 (2 temas, 2 PDFs), confirmando que el refactor no rompió el flujo.

---

## Parte B — UI WPF

### Task 6: Andamiaje del proyecto `Resumenes.Ui` (shell + tema)

**Files:**
- Create: `src/Resumenes.Ui/Resumenes.Ui.csproj`, `App.xaml(.cs)`, `MainWindow.xaml(.cs)`
- Modify: `Resumenes.sln`

**Interfaces:**
- Produces: app WPF que arranca y muestra el shell con `NavigationView` (WPF-UI), tema oscuro/claro.

- [ ] **Step 1: Crear el proyecto y agregarlo a la solución**

Run:

```bash
dotnet new wpf -n Resumenes.Ui -o src/Resumenes.Ui -f net9.0-windows
dotnet sln add src/Resumenes.Ui
dotnet add src/Resumenes.Ui reference src/Resumenes.Core src/Resumenes.Infrastructure
dotnet add src/Resumenes.Ui package WPF-UI
dotnet add src/Resumenes.Ui package CommunityToolkit.Mvvm
```

- [ ] **Step 2: `App.xaml` con recursos de WPF-UI**

Reemplazar `src/Resumenes.Ui/App.xaml`:

```xml
<Application x:Class="Resumenes.Ui.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ui:ThemesDictionary Theme="Dark" />
        <ui:ControlsDictionary />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Application.Resources>
</Application>
```

- [ ] **Step 3: `MainWindow.xaml` con NavigationView**

Reemplazar `src/Resumenes.Ui/MainWindow.xaml`:

```xml
<ui:FluentWindow x:Class="Resumenes.Ui.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Resúmenes de Estudio" Width="1100" Height="720" WindowBackdropType="Mica" ExtendsContentIntoTitleBar="True">
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <ui:TitleBar Grid.Row="0" Title="Resúmenes de Estudio"/>
    <ui:NavigationView x:Name="Nav" Grid.Row="1" PaneDisplayMode="Left">
      <ui:NavigationView.MenuItems>
        <ui:NavigationViewItem Content="Inicio" TargetPageType="{x:Type local:Vistas.VistaInicio}"
            xmlns:local="clr-namespace:Resumenes.Ui">
          <ui:NavigationViewItem.Icon><ui:SymbolIcon Symbol="Home24"/></ui:NavigationViewItem.Icon>
        </ui:NavigationViewItem>
        <ui:NavigationViewItem Content="Configuración" TargetPageType="{x:Type local:Vistas.VistaConfiguracion}"
            xmlns:local="clr-namespace:Resumenes.Ui">
          <ui:NavigationViewItem.Icon><ui:SymbolIcon Symbol="Settings24"/></ui:NavigationViewItem.Icon>
        </ui:NavigationViewItem>
      </ui:NavigationView.MenuItems>
    </ui:NavigationView>
  </Grid>
</ui:FluentWindow>
```

`MainWindow.xaml.cs`: heredar de `Wpf.Ui.Controls.FluentWindow`, en el ctor `InitializeComponent()` y navegar a `VistaInicio` (`Nav.Navigate(typeof(Vistas.VistaInicio))`).

- [ ] **Step 4: Compilar y correr (verificación visual)**

Run: `dotnet run --project src/Resumenes.Ui`
Expected: abre la ventana Fluent oscura con el rail "Inicio / Configuración". Tomar captura.

---

### Task 7: DI, navegación y servicio de la UI

**Files:**
- Create: `src/Resumenes.Ui/Servicios/ServicioNavegacion.cs`, `App.xaml.cs` (host DI)
- Create: `src/Resumenes.Ui/ViewModels/VistaModeloBase.cs`

**Interfaces:**
- Produces: contenedor DI con `IServicioAnalisis`, repo SQLite, secretos, config, navegación; `VistaModeloBase : ObservableObject`.

- [ ] **Step 1: Host de DI en `App.xaml.cs`**

`App.xaml.cs`: usar `Microsoft.Extensions.DependencyInjection` (agregar paquete `dotnet add src/Resumenes.Ui package Microsoft.Extensions.Hosting`). Registrar: `Configuracion` (cargada de settings.json o default con workspace en `%LOCALAPPDATA%`? NO — workspace en el proyecto según decisión; usar la misma resolución que el Cli), `SqliteRepositorioEstado` (singleton, `InicializarEsquema`), `DpapiAlmacenSecretos`, los adaptadores (Deepseek/PaddleOcr/PyMuPdf/PythonGeneradorPdf/LibreOfficeConversor), `ServicioAnalisis`, todas las Vistas y ViewModels. Resolver `MainWindow` y mostrarla en `OnStartup`.

```csharp
// App.xaml.cs (esqueleto)
public partial class App : Application
{
    public static IServiceProvider Servicios { get; private set; } = default!;
    protected override void OnStartup(StartupEventArgs e)
    {
        var sc = new ServiceCollection();
        // ... registrar cfg, repo, secretos, adaptadores, ServicioAnalisis, vistas y VMs ...
        Servicios = sc.BuildServiceProvider();
        Servicios.GetRequiredService<SqliteRepositorioEstado>().InicializarEsquema();
        Servicios.GetRequiredService<MainWindow>().Show();
        base.OnStartup(e);
    }
}
```

(Detallar cada registro siguiendo cómo arma los adaptadores `Program.cs`.)

- [ ] **Step 2: Servicio de navegación**

Create `src/Resumenes.Ui/Servicios/ServicioNavegacion.cs`: envoltorio sobre el `NavigationView` de WPF-UI para navegar entre páginas por tipo y pasar parámetros (p. ej. el `Analisis` en curso). Implementar `Navegar<TVista>(object? parametro = null)`.

- [ ] **Step 3: VM base**

Create `src/Resumenes.Ui/ViewModels/VistaModeloBase.cs`:

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
namespace Resumenes.Ui.ViewModels;
public abstract partial class VistaModeloBase : ObservableObject { }
```

- [ ] **Step 4: Compilar/correr**

Run: `dotnet run --project src/Resumenes.Ui`
Expected: arranca igual; el DI resuelve MainWindow. Captura.

---

### Task 8: Loader pensante + mapeo de progreso (TDD del mapeo)

**Files:**
- Create: `src/Resumenes.Ui/Controles/LoaderPensante.xaml(.cs)`, `src/Resumenes.Ui/Servicios/MapeoProgreso.cs`
- Create: `tests/Resumenes.Ui.Tests/MapeoProgresoTests.cs` (+ proyecto de tests)

**Interfaces:**
- Produces: control `LoaderPensante` (animación + texto de estado); `MapeoProgreso` que convierte `ProgresoPaso` en estado de UI (texto, % global, ítem actual, indeterminado sí/no).

- [ ] **Step 1: Crear el proyecto de tests de UI-logic**

```bash
dotnet new xunit -n Resumenes.Ui.Tests -o tests/Resumenes.Ui.Tests -f net9.0-windows
dotnet sln add tests/Resumenes.Ui.Tests
dotnet add tests/Resumenes.Ui.Tests reference src/Resumenes.Ui src/Resumenes.Core
```

- [ ] **Step 2: Test del mapeo (falla)**

Create `tests/Resumenes.Ui.Tests/MapeoProgresoTests.cs`:

```csharp
using Resumenes.Core.Modelos;
using Resumenes.Core.Orquestacion;
using Resumenes.Ui.Servicios;
using Xunit;

namespace Resumenes.Ui.Tests;

public class MapeoProgresoTests
{
    [Fact]
    public void Avance_con_subindice_es_determinado_y_arma_texto()
    {
        var e = new ProgresoPaso(FaseAnalisis.Limpieza, "a.pdf", 1, 2, Etapa.OcrBruto, "OCR página 2/5", 2, 5, EstadoEvento.Avance);
        var s = MapeoProgreso.AEstado(e);
        Assert.False(s.Indeterminado);
        Assert.Equal(0.4, s.FraccionItem, 3);     // 2/5
        Assert.Contains("OCR", s.Texto);
        Assert.Contains("a.pdf", s.Texto);
    }

    [Fact]
    public void Llamada_IA_sin_subindice_es_indeterminada()
    {
        var e = new ProgresoPaso(FaseAnalisis.Deteccion, "", 0, 0, Etapa.ConsolidacionTemas, "pensando…", null, null, EstadoEvento.Avance);
        var s = MapeoProgreso.AEstado(e);
        Assert.True(s.Indeterminado);
    }
}
```

- [ ] **Step 3: Correr y verificar que falla**

Run: `dotnet test tests/Resumenes.Ui.Tests --filter MapeoProgresoTests`
Expected: FAIL.

- [ ] **Step 4: Implementar `MapeoProgreso`**

Create `src/Resumenes.Ui/Servicios/MapeoProgreso.cs`:

```csharp
using Resumenes.Core.Orquestacion;
namespace Resumenes.Ui.Servicios;

public record EstadoProgreso(string Texto, bool Indeterminado, double FraccionItem, FaseAnalisis Fase, EstadoEvento Estado);

public static class MapeoProgreso
{
    public static EstadoProgreso AEstado(ProgresoPaso e)
    {
        bool indet = e.SubIndice is null || e.SubTotal is null || e.SubTotal == 0;
        double frac = indet ? 0 : (double)e.SubIndice!.Value / e.SubTotal!.Value;
        var item = string.IsNullOrEmpty(e.Item) ? "" : $"{e.Item} — ";
        var texto = $"{item}{(string.IsNullOrEmpty(e.Detalle) ? e.Etapa.ToString() : e.Detalle)}";
        return new EstadoProgreso(texto, indet, frac, e.Fase, e.Estado);
    }
}
```

- [ ] **Step 5: Correr y verificar que pasa**

Run: `dotnet test tests/Resumenes.Ui.Tests --filter MapeoProgresoTests`
Expected: PASS.

- [ ] **Step 6: Control `LoaderPensante`**

Create `src/Resumenes.Ui/Controles/LoaderPensante.xaml`: un `UserControl` con tres puntos que pulsan (animación `Storyboard` en opacidad/escala, en loop) + un `TextBlock` enlazable `Estado`. Propiedad de dependencia `Estado` (string) y `Activo` (bool, arranca/detiene la animación). (Animación pura XAML, no traba el hilo de UI.)

- [ ] **Step 7: Verificación visual**

Poner el control en una página de prueba y correr; verificar que pulsa. Captura.

---

### Task 9: Pantalla "Ejecutando" + VM (TDD del VM)  ⭐

**Files:**
- Create: `src/Resumenes.Ui/Vistas/VistaEjecutando.xaml(.cs)`, `src/Resumenes.Ui/ViewModels/EjecutandoVm.cs`, `ViewModels/ItemProgresoVm.cs`
- Create: `tests/Resumenes.Ui.Tests/EjecutandoVmTests.cs`

**Interfaces:**
- Consumes: `IServicioAnalisis`, `ProgresoPaso`, `MapeoProgreso`.
- Produces: `EjecutandoVm` que corre Fase 1 + Fase 2 con `IProgress`, expone `FaseActual`, `TextoEstado`, `Indeterminado`, `FraccionGlobal`, `Items` (ObservableCollection<ItemProgresoVm>), `Log`, comando `Cancelar`, y evento/propiedad `TemasDetectados` al terminar.

- [ ] **Step 1: Test del VM (falla)** — un `IServicioAnalisis` fake que emite eventos de progreso y devuelve temas; verificar que el VM acumula items, arma el log, y al terminar expone los temas. (Código de test completo: fake servicio + asserts sobre `vm.Items`, `vm.TextoEstado`, `vm.TemasDetectados`.)

```csharp
// tests/Resumenes.Ui.Tests/EjecutandoVmTests.cs
using System.Collections.ObjectModel;
using Resumenes.Core.Interfaces;
using Resumenes.Core.Modelos;
using Resumenes.Core.Orquestacion;
using Resumenes.Ui.ViewModels;
using Xunit;

namespace Resumenes.Ui.Tests;

public class EjecutandoVmTests
{
    private class ServicioFake : IServicioAnalisis
    {
        public Task<Analisis> AbrirOCrearAsync(string c, CancellationToken ct) =>
            Task.FromResult(new Analisis("an1","n","c","fp",EstadoAnalisis.EnProceso,DateTime.UtcNow,DateTime.UtcNow));
        public Task<ResultadoLote> ProcesarArchivosAsync(Analisis an, IProgress<ProgresoPaso>? p, CancellationToken ct)
        {
            p?.Report(new ProgresoPaso(FaseAnalisis.Limpieza,"a.txt",1,1,Etapa.LimpiezaIA,"pensando…",null,null,EstadoEvento.Iniciado));
            p?.Report(new ProgresoPaso(FaseAnalisis.Limpieza,"a.txt",1,1,Etapa.LimpiezaIA,"",null,null,EstadoEvento.Completado));
            return Task.FromResult(new ResultadoLote(1,0,Array.Empty<string>()));
        }
        public Task<IReadOnlyList<TemaDetectado>> DetectarTemasAsync(Analisis an, string pt, CancellationToken ct) =>
            Task.FromResult<IReadOnlyList<TemaDetectado>>(new[]{ new TemaDetectado("t1","Tema 1",1,new(){"a"}) });
        public Task<ResultadoLote> GenerarPorTemasAsync(Analisis an, IReadOnlyList<TemaDetectado> t, IProgress<ProgresoPaso>? p, CancellationToken ct)
            => Task.FromResult(new ResultadoLote(t.Count,0,Array.Empty<string>()));
    }

    [Fact]
    public async Task Ejecuta_fase1_y_deteccion_y_expone_temas()
    {
        var an = new Analisis("an1","n","c","fp",EstadoAnalisis.EnProceso,DateTime.UtcNow,DateTime.UtcNow);
        var vm = new EjecutandoVm(new ServicioFake());
        await vm.EjecutarAsync(an, "");
        Assert.NotEmpty(vm.Log);
        Assert.Single(vm.TemasDetectados);
        Assert.Equal("Tema 1", vm.TemasDetectados[0].Nombre);
    }
}
```

- [ ] **Step 2: Correr y verificar que falla**

Run: `dotnet test tests/Resumenes.Ui.Tests --filter EjecutandoVmTests`
Expected: FAIL.

- [ ] **Step 3: Implementar `ItemProgresoVm` y `EjecutandoVm`**

`ItemProgresoVm`: `[ObservableProperty]` Nombre, Estado (chip), Fraccion. `EjecutandoVm`: ctor `(IServicioAnalisis)`; método `EjecutarAsync(Analisis an, string promptTemas)` que crea un `Progress<ProgresoPaso>` (en el hilo de UI) que: actualiza/crea items por `e.Item`, setea `TextoEstado`/`Indeterminado`/`FraccionItem` vía `MapeoProgreso`, y agrega al `Log`. Llama `ProcesarArchivosAsync` → `DetectarTemasAsync`, expone `TemasDetectados`. Comando `Cancelar` con `CancellationTokenSource`. (Código completo en la implementación.)

- [ ] **Step 4: Correr y verificar que pasa**

Run: `dotnet test tests/Resumenes.Ui.Tests --filter EjecutandoVmTests`
Expected: PASS.

- [ ] **Step 5: `VistaEjecutando.xaml`**

Layout: stepper de fases arriba (3 chips), `LoaderPensante` (visible cuando `Indeterminado`), `ui:ProgressRing`/barra determinada (cuando no), `ItemsControl` con los `Items` (chip de estado + mini-barra), barra global + tiempo + botón Cancelar, y un `Expander` con el `Log`. Bindings al `EjecutandoVm`.

- [ ] **Step 6: Verificación visual con un análisis real (TXT)**

Cablear el flujo mínimo (Inicio→Configurar→Ejecutando) lo suficiente para correr `tests/fixtures/temas` desde la UI y ver el progreso. Captura del progreso en vivo.

---

### Task 10: Inicio (historial) + Configurar análisis

**Files:**
- Create: `Vistas/VistaInicio.xaml(.cs)`, `ViewModels/InicioVm.cs`, `ViewModels/AnalisisHistorialVm.cs`
- Create: `Vistas/VistaConfigurar.xaml(.cs)`, `ViewModels/ConfigurarVm.cs`
- Test: `tests/Resumenes.Ui.Tests/InicioVmTests.cs`

**Interfaces:**
- Produces: `InicioVm` (historial vía `repo.ListarAnalisis()`, comando NuevoAnalisis, acciones por ítem); `ConfigurarVm` (seleccionar carpeta, listar archivos, prompt temas, comando Analizar → navega a Ejecutando).

- [ ] **Step 1: Test `InicioVm` (falla)** — repo fake con 2 análisis; `vm.Analisis` tiene 2 y respeta el orden. (Código completo del test.)
- [ ] **Step 2: Correr (falla).**
- [ ] **Step 3: Implementar `AnalisisHistorialVm` + `InicioVm`** (carga `ListarAnalisis`, comandos AbrirCarpeta/VerResultados/Eliminar/Continuar, NuevoAnalisis navega a Configurar).
- [ ] **Step 4: Correr (pasa).**
- [ ] **Step 5: `VistaInicio.xaml`** (título+fecha+saludo, botón grande Nuevo análisis, `ItemsControl` de tarjetas con `ui:Card`).
- [ ] **Step 6: `ConfigurarVm` + `VistaConfigurar.xaml`** (folder picker con `OpenFolderDialog` de WPF, `TextBox` para prompt de temas, lista de archivos detectados, botón Analizar que navega a Ejecutando pasando carpeta+prompt).
- [ ] **Step 7: Verificación visual** (Inicio muestra historial; Configurar selecciona carpeta y lista archivos). Capturas.

---

### Task 11: Confirmar temas + Generando + Resultados

**Files:**
- Create: `Vistas/VistaConfirmarTemas.xaml(.cs)`, `ViewModels/ConfirmarTemasVm.cs`, `ViewModels/TemaEditableVm.cs`
- Create: `Vistas/VistaGenerando.xaml(.cs)` (reusa el progreso de Ejecutando para Fase 3), `Vistas/VistaResultados.xaml(.cs)`, `ViewModels/ResultadosVm.cs`
- Test: `tests/Resumenes.Ui.Tests/ConfirmarTemasVmTests.cs`

**Interfaces:**
- Produces: `ConfirmarTemasVm` (lista `TemaEditableVm` editable: renombrar/quitar/fusionar/reasignar; comando Generar → escribe temas.json y navega a Generando); `ResultadosVm` (lista temas+PDF, comandos AbrirPdf/AbrirCarpeta/Exportar, reproceso).

- [ ] **Step 1: Test `ConfirmarTemasVm` (falla)** — editar nombre de un tema, quitar un tema, y `ObtenerTemasConfirmados()` refleja los cambios. (Código completo.)
- [ ] **Step 2: Correr (falla).**
- [ ] **Step 3: Implementar `TemaEditableVm` + `ConfirmarTemasVm`** (carga los temas detectados; comandos Renombrar/Quitar/Fusionar/ReasignarArchivo; `ObtenerTemasConfirmados()` → `IReadOnlyList<TemaDetectado>`; comando Generar persiste y navega).
- [ ] **Step 4: Correr (pasa).**
- [ ] **Step 5: XAML de ConfirmarTemas** (lista editable de temas con sus archivos; botones renombrar/quitar/fusionar; botón "Generar resúmenes").
- [ ] **Step 6: VistaGenerando** (instancia el progreso de Fase 3 llamando `GenerarPorTemasAsync` con el mismo patrón del `EjecutandoVm`; al terminar navega a Resultados).
- [ ] **Step 7: `ResultadosVm` + `VistaResultados.xaml`** (lista de temas con su PDF en `final/`; `AbrirPdf` con `Process.Start(pdf, UseShellExecute)`; `AbrirCarpeta`; `Exportar` copia los PDFs a una carpeta elegida; reproceso = borrar el estado de la etapa y re-correr).
- [ ] **Step 8: Verificación visual del flujo completo** (Configurar→Ejecutando→ConfirmarTemas→Generando→Resultados) sobre `tests/fixtures/temas`. Capturas de cada pantalla.

---

### Task 12: Configuración + Onboarding (primer arranque)

**Files:**
- Create: `Vistas/VistaConfiguracion.xaml(.cs)`, `ViewModels/ConfiguracionVm.cs`
- Create: `Vistas/VistaOnboarding.xaml(.cs)`, `ViewModels/OnboardingVm.cs`

**Interfaces:**
- Produces: `ConfiguracionVm` (editar API key [DPAPI], carpeta salida, DPI, modelo, tema claro/oscuro+acento, persistir en settings.json); `OnboardingVm` (primer arranque: si falta API key o carpeta, guía a cargarlas; la descarga de dependencias se integra en el sub-proyecto Instalador — acá queda el placeholder de estado del runtime con verificación de presencia).

- [ ] **Step 1: `ConfiguracionVm` + vista** — campos enlazados; comando Guardar escribe settings.json; comando GuardarApiKey usa `IAlmacenSecretos`; selector de tema aplica `ApplicationThemeManager.Apply(...)` de WPF-UI.
- [ ] **Step 2: `OnboardingVm` + vista** — chequea presencia de runtime (scripts/fuentes/soffice/modelos) y API key/carpeta; muestra el estado y guía; si todo OK, marca "listo". (La descarga real se conecta en el Instalador.)
- [ ] **Step 3: Lógica de arranque** — en `App.OnStartup`, si falta config esencial → navegar a Onboarding; si no → Inicio.
- [ ] **Step 4: Verificación visual** — Configuración cambia tema/acento en vivo; primer arranque muestra onboarding. Capturas.

---

### Task 13: Integración final y verificación

**Files:**
- Modify: wiring final de navegación entre todas las vistas.

- [ ] **Step 1: Correr toda la suite**

Run: `dotnet test`
Expected: PASS (backbone + UI VM tests).

- [ ] **Step 2: Verificación end-to-end de la app**

Run: `dotnet run --project src/Resumenes.Ui`
Flujo: Inicio → Nuevo análisis → seleccionar `tests/fixtures/temas` → Ejecutando (ver progreso pensante + items + log) → Confirmar temas (renombrar uno) → Generando → Resultados (abrir un PDF). Tomar capturas de cada paso.

- [ ] **Step 3: Verificar criterios de aceptación del spec (sección 7)** uno por uno con la app real; anotar evidencia (capturas).

---

## Self-Review

**1. Spec coverage:** Arquitectura/refactor (Tasks 1–5); pantallas: Inicio (10), Configurar (10), Ejecutando (9), ConfirmarTemas (11), Generando (11), Resultados (11), Configuración (12), Onboarding (12); progreso pensante (8,9); errores/threading (en EjecutandoVm/ServicioAnalisis); historial (2,10); tema claro/oscuro (12). Criterios de aceptación → Task 13. **Cubierto.**

**2. Placeholder scan:** los pasos de UI de XAML describen el layout concreto a construir (no "TBD"); la lógica testeable (VMs, mapeo, repo, orquestador) trae código completo. Las vistas se validan con capturas (no hay test de UI automatizado, por decisión del spec). Tareas 5/10/11/12 referencian "código completo en la implementación" para los VM/XAML extensos: el implementador debe escribirlos siguiendo las firmas de Interfaces y los tests provistos.

**3. Type consistency:** `IServicioAnalisis` (Task 5) usado igual en EjecutandoVm/ConfirmarTemas/Generando. `ProgresoPaso`/`ContextoPaso` (Task 1) consistentes en orquestador (3), adaptadores (4), ConstructorPipeline (5), MapeoProgreso (8), EjecutandoVm (9). `TemaDetectado` movido a Core (5) y usado por el servicio y los VMs. `ListarAnalisis` (2) usado en InicioVm (10).

## Notas de implementación

- **Sin git:** ignorar cualquier instinto de commit.
- **`ConstructorPipeline` y `DetectorTemas` se mueven de `Resumenes.Cli` a `Resumenes.Infrastructure/Aplicacion/`** (Task 5) para que `ServicioAnalisis` los use; actualizar `using` y el `Program.cs`.
- **`SlugId`/`TipoDe`** se extraen de `Program.cs` a `Core/Apoyos/Ids.cs` (compartidos por Cli y servicio).
- WPF-UI: si la versión del paquete cambia algún namespace/símbolo, ajustar (la API es estable en 3.x/4.x).
- El workspace y la API key siguen la misma resolución que el Cli (workspace en el proyecto; key en `%LOCALAPPDATA%`).
